#version 400

uniform sampler2D Tex1;

in vec3 LightIntensity;

out vec4 FragColor;

void main() {
vec4 texColor = texture(Tex1, gl_TexCoord[0].st);
FragColor = texColor * vec4(LightIntensity, 1.0);
}